local Json = class("Json")

function Json:init()
    csjson = JsonUtil.New()
    iter = csjson.JsonDic:GetEnumerator()
    while iter:MoveNext() do
        local key = iter.Current.Key
        local value = iter.Current.Value
        self[key] = value
    end
end

return Json
