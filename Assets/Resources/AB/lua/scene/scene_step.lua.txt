local base = require("scene_base")
local Scene = class("step", base)

function Scene:onLoad()
    if GameConst.PRO_ENV == ENV_TYPE.MASTER then
        ui:loadScene("UpRes")
    else
        --test
        --ui:loadScene("Login")
        ui:loadScene("UpRes")
    end

    print(ui:showUI("ui_tip", {msg = "fjlsadjfk"}))
end
return Scene
