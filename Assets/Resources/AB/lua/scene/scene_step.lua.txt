local base = require("scene_base")
local SceneLogin = class("SceneStep", base)

function SceneLogin:onLoad()
    --ui:loadScene("UpRes");

    --test

    local common_pb = require "common_pb"
    local http_pb = require "http_pb"

    local data = pb:serialize("common_pb.Error", {code = 500, msg = "error"})

    print(tostring(data))
    print(tostring(pb:deserialize("common_pb.Error", data)))

    local data = pb:serialize("http_pb.s2c", {Error = {code = 500, msg = "error"}})

    print(tostring(data))
    print(tostring(pb:deserialize("http_pb.s2c", data)))
    --ui:loadScene("Login")
end
return SceneLogin
